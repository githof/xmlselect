
## taggable_xml

  + ids
    utiliser la méthode get_id()
    - pour les form : acte_epouse_mere_form
    - pour les input : acte_epouse_mere_input_prenom
    - test sur un noeud simple
    - test sur tout l'acte

  + brancher les select_and_show
    - attributs $source et $show pour taggable_xml
    - attribut sel_show, création d'un select_and_show
    - test sur un text_node
    - test sur un tag_text_node
    - test sur un xml_node qui contient deux trois tag_text_node et text_node
    - test sur tout l'acte

## tag_set

   remplacer choice_tags par l'utilisation d'un dictionnaire sur le
   modèle de wedding_tags.
   -> au lieu de piocher les tags dans une liste fixe, on les pioche
   dans tags[that.tag]

   - remplir wedding_tags
   - passer le dictionnaire de tags en paramètre de l'objet
      taggable_xml
      (NB : ça pourrait être pertinent d'en faire plutôt un attribut
      de xml_node, mais pas sûr que ce soit vraiment utile)
   - dans taggable_xml, quand tags[that.tag] est vide, alors on ne
      crée pas le formulaire, on laisse juste un champ texte, dans une
      section qui a pour classe 'leaf' en plus de 'text_node'

# À vérifier

- les erreurs sur le range de select_and_show à la création

# Suite

+ premier input radio sélectionné par défaut




   
